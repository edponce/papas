sudo: false

language: python

branches:
    only:
        - master

matrix:
    - TOXENV: py34
    - TOXENV: py35
    - TOXENV: flake8

install:
    - pip install tox

build: false

test_script:
    - tox
