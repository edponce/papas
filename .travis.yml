sudo: false

language: python

branches:
    only:
        - master

matrix:
    include:
        - python: 3.4
          env: TOXENV=py34
        - python: 3.5
          env: TOXENV=py35
        - TOXENV: flake8

install:
    - python3 -m pip install tox

build: false

script:
    - tox
