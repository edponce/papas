sudo: false

branches:
    only:
        - master

language: python

python:
    - '3.5'

install:
    - sudo apt install libopenmpi-dev openmpi-bin
    - pip install tox-travis flake8 coverage

script:
    - tox
