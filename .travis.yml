sudo: false

language: python

branches:
    only:
        - master

matrix:
    - TOXENV: py34
    - TOXENV: py35
    - TOXENV: flake8

install:
    - python3 -m pip install tox

build: false

script:
    - tox
