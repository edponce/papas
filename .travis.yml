sudo: false

branches:
    only:
        - master

language: python

python:
    - '3.5'
    - '3.6'

install:
    - sudo apt install libopenmpi-dev openmpi-bin
#    - pip install tox tox-travis nose flake8 coverage

script:
    - tox
